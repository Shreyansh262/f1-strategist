"""Phase 5 — RL pit-strategy agent (STRETCH).

A Gymnasium environment that wraps the project's race simulation (one ego
driver against a fixed rival field) and a PPO trainer + head-to-head evaluator
versus the Phase 4a MDP policy. See src/rl/env.py for the design notes.
"""
